num_train_steps: 5e6
num_train_iters: 1
device: cuda:0
#device: cpu
seed: 129
num_envs: ${agent.num_envs}

log_frequency_step: 1e2
log_save_tb: true
save_video: true
eval_frequency_step: 1e4
num_eval_episodes: 32

defaults:
#  - env: atari
  - env: mazelab
#  - env: tetris
#  - agent: a2c
#  - agent: a2c_rnn
#  - agent: a2c_crnn
  - agent: a2c_2am
#  - agent: ppo
#  - agent: ppo_rnn
#  - agent: ppo_crnn

# hydra configuration
hydra:
  name: ${env.env_id}
  run:
    dir: ./runs/${now:%Y.%m.%d}/${now:%H%M%S}_${hydra.job.override_dirname}
  sweep:
    dir: ./multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}_${hydra.job.num}